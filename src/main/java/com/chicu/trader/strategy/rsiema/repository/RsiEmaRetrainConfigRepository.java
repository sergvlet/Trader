package com.chicu.trader.strategy.rsiema.repository;

import com.chicu.trader.strategy.rsiema.model.RsiEmaRetrainConfig;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RsiEmaRetrainConfigRepository extends JpaRepository<RsiEmaRetrainConfig, Long> {
}
