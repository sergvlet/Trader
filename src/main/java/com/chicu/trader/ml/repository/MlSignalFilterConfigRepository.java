package com.chicu.trader.ml.repository;

import com.chicu.trader.ml.model.MlSignalFilterConfig;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MlSignalFilterConfigRepository extends JpaRepository<MlSignalFilterConfig, Long> {
}
